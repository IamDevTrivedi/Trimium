{
    "$schema": "https://opencode.ai/config.json",
    "default_agent": "trimium",
    "agent": {
        "trimium": {
            "description": "Primary agent specialized for Trimium development — knows the full monorepo stack, conventions, and patterns",
            "mode": "primary",
            "prompt": "{file:./AGENTS.md}",
            "permission": {
                "edit": "allow",
                "bash": "allow",
                "read": "allow"
            }
        },
        "review": {
            "description": "Reviews code for Trimium-specific best practices, security, and performance",
            "mode": "subagent",
            "permission": {
                "edit": "deny",
                "bash": "deny"
            }
        }
    },
    "instructions": [
        "AGENTS.md",
        "docs/DIRECTORY_STRUCTURE.md",
        "docs/SETUP.md",
        "docs/architecture/AUTHENTICATION_AND_SESSION_MANAGEMENT_ARCHITECTURE.md",
        "docs/architecture/CI_CD_PIPELINE_ARCHITECTURE.md",
        "docs/architecture/RATE_LIMITER_ARCHITECTURE.md"
    ],
    "references": {
        "docs": {
            "path": "docs",
            "description": "Use for project architecture, setup guides, and design decisions"
        }
    },
    "command": {
        "check": {
            "template": "Run pnpm check (lint + format) and fix any issues",
            "description": "Run lint and format check"
        },
        "build-server": {
            "template": "Build the server with `pnpm --filter trimium-server build`",
            "description": "Build server to /server/dist/"
        },
        "build-client": {
            "template": "Build the client with `pnpm --filter trimium-client build`",
            "description": "Build Next.js app to /client/.next/"
        }
    }
}
